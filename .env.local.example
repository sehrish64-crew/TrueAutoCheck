
EMAIL_USER=autofactschecks@gmail.com
EMAIL_PASS=YOUR_APP_PASSWORD_HERE
EMAIL_TO=autofactschecks@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=autofactschecks@gmail.com
SMTP_PASS=YOUR_APP_PASSWORD_HERE
EMAIL_FROM="Auto Facts Check <autofactschecks@gmail.com>"
# Gmail app passwords must be pasted as the 16-character code without spaces.
# If Gmail keeps failing, use a dedicated SMTP provider instead:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=YOUR_SENDGRID_API_KEY

# Local DB (if using local MySQL)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=carvertical
DB_USER=root
DB_PASSWORD=

NODE_ENV=development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# SMTP (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=autofactschecks@gmail.com
SMTP_PASS=YOUR_APP_PASSWORD_HERE
# Gmail app passwords must be pasted as the 16-character code without spaces.
