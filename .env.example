# Example environment variables
# Add your own variables here

# SMTP (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=yourgmail@gmail.com
SMTP_PASS=your_app_password
SMTP_SECURE=true

# Optional: Resend API key (alternative to SMTP)
# RESEND_API_KEY=

# Default from address and admin email (set to your admin email)
EMAIL_FROM="Vehicle Reports <no-reply@yourdomain.com>"
ADMIN_EMAIL=sehrishnadeem39@gmail.com

# Admin UI secret for viewing /api/emails in production (optional)
# ADMIN_SECRET=supersecrettoken

# NEXT_PUBLIC_BASE_URL is used for building links in emails during local testing
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
