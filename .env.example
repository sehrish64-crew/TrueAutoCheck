# Example environment variables (do NOT commit real secrets)
# Copy this to .env.local and fill values locally.

# Paddle (frontend client token - ctok_... for browser)
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=ctok_YOUR_CLIENT_TOKEN_HERE
NEXT_PUBLIC_PADDLE_ENV=sandbox

# Database
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=carvertical

# Admin
ADMIN_EMAIL=admin@example.com
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASS=ChangeMe123!

# SMTP (fill with your SMTP provider credentials — do NOT commit real values)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Vehicle Reports <no-reply@example.com>"

# Optional: Resend API
RESEND_API_KEY=

# Base URL
NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# External APIs
AUTO_DEV_API_KEY=

# Paddle server keys (server only - pdl_ keys must NOT be exposed)
PADDLE_VENDOR_ID=
PADDLE_API_KEY=
PADDLE_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...your paddle public key...\n-----END PUBLIC KEY-----"

# Local environment flag
PADDLE_ENV=sandbox
# Example environment variables
# Add your own variables here

# SMTP (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=yourgmail@gmail.com
SMTP_PASS=your_app_password
SMTP_SECURE=true
ADMIN_EMAIL=your-admin@example.com
PADDLE_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...your paddle public key...\n-----END PUBLIC KEY-----"
EMAIL_FROM="Vehicle Reports <no-reply@yourdomain.com>"
ADMIN_EMAIL=sehrishnadeem39@gmail.com